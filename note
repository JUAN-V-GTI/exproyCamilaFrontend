*********requerimientos de mi proyecto************
necesito crear la nueva  estructura de  mi proyecto para las carpetas y archivos que se creran
por ejemplo  como los screens, components contants hooks navigation services etc   
crear un Login un register y un home 
como pantalla principal estará el login 
consumir los endpoint que ccreados en el backend que esta desarrollado con springboot y JWT  

*********datos de endpoint controller************
; // @RequestMapping("/auth")
; // @CrossOrigin(origins = "http://localhost:8080")
; // public class AuthController {
; //    @PostMapping(value = "login")
; //    @PostMapping(value = "register")
                                 
*********datos requeridos para los endpoint************
; //  LoginRequest 
; //      username;
; //      password;

; //  RegisterRequest 
; //      username;
; //      password;
; //      firstname;
; //      lastname;
; //      country;
; //      Role;




*********crear proyecto************
npx create-expo-app@latest exproy-camila-front-app --template blank

*********config proyecto************
npx expo install react-dom react-native-web @expo/metro-runtime
npx expo lint

*********dependencias************
npx expo install -- --save-dev prettier eslint-config-prettier eslint-plugin-prettier
npx expo  install expo-constants
npx expo install react-native-safe-area-context
npx expo install react-native-svg npx expo install react-native-svg
npx expo install @react-navigation/native @react-navigation/stack
npx expo install react-native-screens react-native-safe-area-context
npx expo install react-native-elements
npx expo install react-native-vector-icons
expo install @expo/vector-icons
npm install react-native-gesture-handler 
npm install react-native-reanimated 
npm install -g npm@latest 
npm install @react-native-community/viewpager
npm install @react-native-community/unsafe-performant-view
npm install react-native-snap-carousel
npm install react-native-reanimated react-native-gesture-handler
npm install react-native-linear-gradient



*********estructura del proyecto************
EXPROY-CAMILA-FRONT-APP
├── assets
├── node_modules
├── src
│   ├── components
│   ├── constants
│   ├── hooks
│   ├── navigation
│   ├── screens
│   │   ├── HomeScreen.js
│   │   ├── LoginScreen.js
│   │   └── RegisterScreen.js
│   ├── services
│   │   ├── auth.js
│   └── AppNavigator.js
├── styles
├── .eslintrc.js
├── .gitignore
├── App.js
├── app.json
├── babel.config.js
├── package-lock.json
├── package.json
└── note

*********crear carpetas y archivos***********
mkdir src
mkdir src/screens
mkdir src/components
mkdir src/constants
mkdir src/hooks
mkdir src/navigation
mkdir src/services

New-Item -ItemType File -Path src/screens/HomeScreen.js
New-Item -ItemType File -Path src/screens/LoginScreen.js
New-Item -ItemType File -Path src/screens/RegisterScreen.js
New-Item -ItemType File -Path src/services/Auth.js
***********uso de la aplicacion Expogo***************

Press s │ switch to development build:
Al presionar la tecla “s”, cambiarás a la versión de desarrollo de tu aplicación.
La versión de desarrollo incluye herramientas de depuración y permite ver los errores y advertencias en la consola.

Press j │ open debugger:
Al presionar la tecla “j”, abrirás el depurador de JavaScript.
Puedes usar esto para inspeccionar el código, establecer puntos de interrupción y depurar problemas.

Press r │ reload app:
Al presionar la tecla “r”, recargarás la aplicación.
Útil para aplicar cambios en tiempo real mientras desarrollas.

Press m │ toggle menu:
Al presionar la tecla “m”, mostrarás u ocultarás el menú de desarrollo.
El menú contiene opciones adicionales para depurar y configurar tu aplicación.

Press o │ open project code in your editor:
Al presionar la tecla “o”, abrirás el código fuente de tu proyecto en tu editor de código configurado.
Esto te permite realizar cambios en el código directamente desde tu editor.